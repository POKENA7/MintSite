import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#d87274',
      light: '#ffa2a3',
      dark: 'lightblue',
    },
  },
})

export default theme
